# MiniSpringBoot 后端 demo 默认配置（M4 外部化配置字段 + M6 自动配置开关 + M7 应用字段合并）
app:
  name: "MiniSpringBoot"
  timeout: 30
  features:
    - ioc
    - aop
  pointer: name
server:
  port: "9090"
smtp:
  host: localhost
  port: 25
# M6 自动配置演示：optionalFeature 开关
feature:
  optional:
    enabled: "true"