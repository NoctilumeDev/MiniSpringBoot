# 安全策略

## 支持范围

当前仅维护默认分支和最新已发布里程碑。历史 tag 用于教学回溯与工程证据，除非问题仍然影响当前版本，否则不单独回补。

MiniSpringBoot 是教学框架与本地验证项目，不是开箱即用的生产框架。公开部署前，使用者仍需自行完成身份认证、权限控制、密钥管理、TLS、网络隔离和运行环境加固。

## 报告安全问题

请不要在公开 Issue、Discussion、截图或日志中披露尚未修复的漏洞、可直接利用的完整步骤或任何真实凭据。

优先通过仓库 `Security` 页面中的 GitHub Private Vulnerability Reporting 私下报告。若该入口不可用，可以只创建一条不含漏洞细节的 Issue，请求仓库所有者提供私密沟通渠道。

报告最好包含：

- 受影响的模块、提交或版本；
- 最小复现条件与预期/实际结果；
- 可观察到的影响范围；
- 已尝试的缓解方式（如有）。

维护者会先确认问题是否影响当前支持范围，再决定修复、临时缓解和披露方式。本个人项目不承诺商业 SLA，但不会把未验证、结果未知或安全失败描述为成功。

## 演示与证据边界

仓库中的本地 MySQL、演示账号、负例夹具、故障注入和 VeriTrail Bundle 仅用于可复现教学与验收，不得连接真实用户数据或复用生产密钥。提交报告前请移除 Token、Cookie、密码、代理订阅、内网地址和个人数据。
